#include "main.h"
#include <stdio.h>

/**
 * mul - multiplication of two number
 * @a: multiplied with b
 * @b: multiplied with a
 *
 * Return: sum
 */
int mul(int a, int b)
{
	int sum;

	sum = a * b;

	return (sum);
}
